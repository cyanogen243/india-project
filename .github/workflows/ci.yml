name: Validate standalone repository

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      VERIFY_COMMITTED_FEED_ONLY: "true"

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - run: npm ci
      - run: npm test
      # test:sites builds the Worker artifact and then imports it. Running only
      # the build passed while the artifact was unloadable: a native module at
      # import scope builds fine and fails when the Worker starts.
      - run: npm run test:sites
      - run: npm exec tsc -- --noEmit
      - run: npm run lint
